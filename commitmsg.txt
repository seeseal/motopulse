feat: glassmorphism UI overhaul + dashboard redesign for clarity

- GlassCard widget with BackdropFilter blur for iOS-style frosted glass
- GradientBackground widget replaces flat #080808 with deep radial gradient
- Dashboard fully redesigned: MP logo mark, app tagline chip, hero START RIDE
  button, 2x2 quick-action grid (SOS/Group/Speedo/Maintenance), glass stat cards
- Glass nav bar with frosted blur, SOS tab gets red pill treatment
- Ride tracking stats panel uses BackdropFilter glass background
- Speed, distance, max speed, fuel cards all converted to GlassCard
- dart:ui import added to ride_tracking_screen for ImageFilter